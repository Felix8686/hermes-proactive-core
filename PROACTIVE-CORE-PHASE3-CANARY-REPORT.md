# Proactive Core Phase 3 Canary Report

Date: 2026-09-17 (Asia/Singapore)  
Repository: `Felix8686/hermes-proactive-core`  
Branch: `codex/proactive-core-v1`  
Draft PR: [#1](https://github.com/Felix8686/hermes-proactive-core/pull/1)

## Result

Phase 1–2 was synchronized verbatim, tested in the repository checkout, committed, and pushed. The VPS Canary was **not started**: the handoff requires all three feature switches to remain false, while the current Shadow entry point returns before reading any input when `PROACTIVE_ENABLED=false`. Enabling that switch for a manual run would contradict the handoff. No code path was added to bypass the kill switch.

## Required fields

```text
PHASE1_2_IMPORTED_TO_GITHUB = PASS
REPO_TESTS = PASS
PROACTIVE_CORE_GIT_REPO = RESOLVED
CANARY_DEPLOYMENT = FAIL
PRODUCTION_FILES_CHANGED = 0
PRODUCTION_SERVICES_RESTARTED = 0
LLM_CALLS = 0
TELEGRAM_MESSAGES_SENT = 0
ACTIONS_EXECUTED = 0
DEDUPE_PERSISTENCE = FAIL
RECOVERY_HANDLING = FAIL
KILL_SWITCHES = PASS
GATEWAY_REGRESSION = FAIL
CRON_REGRESSION = FAIL
EXECUTOR_REGRESSION = FAIL
OPENVIKING_REGRESSION = FAIL
PHASE3_CANARY = FAIL
GATE_PROACTIVE_CORE_NOTIFICATION_REVIEW = WAITING_FOR_CHATGPT
```

For `DEDUPE_PERSISTENCE`, `RECOVERY_HANDLING`, and the four regression fields, `FAIL` means the Phase 3 canary evidence was not collected because no canary window ran; it does **not** assert that a production regression occurred. The Phase 1–2 local suite covers dedupe and recovery and passed.

## Phase 1–2 import and test evidence

- Import commit: `c77a55f` (`feat: import proactive core phase 1-2`), pushed to `codex/proactive-core-v1`; Draft PR #1 remains open and unmerged.
- The 17 implementation/test files under `proactive-core-v1/` matched the source tree by relative path and SHA-256 before testing. The five design and Phase 1–2 report documents under `docs/` also matched their source files by SHA-256.
- Repository-checkout test command: `python -B -m unittest discover -s tests -v` from `proactive-core-v1/`.
- Result: **33/33 passed**.
- The post-test `.pyc` cache created by test subprocesses was removed before the import commit; no generated bytecode is committed.
- Code defaults for `PROACTIVE_ENABLED`, `PROACTIVE_NOTIFICATIONS_ENABLED`, and `PROACTIVE_ACTIONS_ENABLED` are all false in `proactive-core-v1/proactive_core/switches.py` (lines 24–36). The local test suite passed with the disabled behavior covered.

## Why the VPS Canary could not proceed

1. The handoff requires all three switches to stay false throughout Phase 3.
2. The CLI requires `--enable-shadow`, then exits at `proactive-core-v1/run_shadow.py` lines 38–48 when `PROACTIVE_ENABLED` is false. `ShadowDetector.run()` independently returns an empty result at `proactive-core-v1/proactive_core/shadow.py` lines 50–59 when that switch is false.
3. The existing entry point accepts only JSON fixtures directly inside its own `fixtures/` directory (`run_shadow.py` lines 21–28); it has no production-source adapters. No VPS deployment, input normalization, state write, SSH session, or production status query was attempted after identifying the switch conflict.

The safe next decision for ChatGPT review is whether to amend the Canary contract to permit `PROACTIVE_ENABLED=true` **only for a manually invoked isolated Shadow process**, while keeping notifications and actions false and retaining all other no-change constraints, or to keep all three switches false and defer Canary execution. No such exception was assumed here.

## Production and model boundary

No VPS connection was made in this phase attempt. Consequently, no production file or service was changed, no service was restarted, and the active provider/model was not re-read. The prior Phase 1–2 report records `OpenAI Codex / gpt-5.6-luna`; this attempt did not alter it. No Telegram send or ACT executor was invoked.

## Gate

`GATE_PROACTIVE_CORE_NOTIFICATION_REVIEW = WAITING_FOR_CHATGPT`

STOP. Do not deploy a Canary, enable proactive Telegram delivery, enable ACT, modify the active Hermes model, or merge `main` until ChatGPT reviews this report and resolves the switch-contract conflict.
